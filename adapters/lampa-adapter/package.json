{
  "name": "lampa-adapter",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --watch src --watch static --ext ts,js --signal SIGTERM --exec \"node --loader ts-node/esm src/index.ts\"",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "nodemon": "^3.1.0"
  }
}
